# 📷 실시간 웹캠 기반 생체 정보 분석 및 낙상 감지 시스템

본 프로젝트는 실시간 웹캠 피드를 활용하여 사용자의 거리에 따라 두 가지 모드(생체 정보 분석 / 낙상 감지)를 자동으로 전환하며 동작하는 하이브리드 비전 시스템입니다. 향후 엣지 디바이스(Jetson Nano 등) 확장을 고려하여 객체 지향적으로 설계되었습니다.

## ⚙️ 시스템 구동 방식 (Execution Flow)
1. **거리 판별 루프 (Main Loop):** 웹캠 프레임 입력 시 안면 랜드마크를 추출하여, 두 눈 사이의 거리($d_{eyes}$) 또는 얼굴 ROI 바운딩 박스의 면적을 계산합니다.
2. **모드 스위칭:**
   - **원거리 (거리 임계치 미달):** `기능 2 (낙상 감지)` 실행
   - **근거리 (거리 임계치 도달):** `기능 1 (생체 정보 분석)` 실행
3. **병렬 처리 아키텍처:** 영상 입력 및 딥러닝 추론(MediaPipe 등)은 GPU를 활용하고, 도출된 좌표 및 결과값을 활용한 상태 판단(수학적 연산)은 CPU에서 처리합니다.

---

## 🛠️ 기능 정의서

### 기능 1: 안면 ROI 기반 생체 정보 측정 (Vital & Texture Analysis)
사용자가 카메라에 충분히 가까이 다가왔을 때 작동하며, 건강 상태를 모니터링합니다.
- **안면 인식 및 ROI 지정:** 얼굴 영역을 탐지하여 분석 영역(ROI)을 특정합니다.
- **심박수(Heart Rate) 측정:** 오픈소스 rPPG 모듈을 연동하여, 얼굴 영역의 미세한 색상 변화를 통해 실시간 심박수를 계산합니다.
- **피부 질감 및 상태 변화 분석:**
  - 최초 인식된 프레임(Baseline)의 피부 상태를 저장합니다.
  - **홍조 변화량:** 색상 변환 모듈(HSV 등)을 통해 붉은기(Redness) 수치 변화를 추적합니다.
  - **발한(Sweatness) 변화량:** 질감 분석 모듈을 통해 픽셀의 난반사(Specular reflection) 및 수분감 변화를 연산하여 땀 분비 여부를 측정합니다.

### 기능 2: 관절 좌표 기반 낙상 감지 (Fall Detection)
사용자가 카메라에서 멀리 떨어져 있을 때 작동하며, 실내 안전 사고를 감지합니다. 
MediaPipe를 통해 추출된 어깨($y_{shoulder}$), 골반($y_{pelvis}$), 무릎($y_{knee}$)의 픽셀 좌표($y$축)를 기준으로 자세를 분류합니다.

*조건: 이미지 해상도 $H=640$ 픽셀 기준*
- **(a) 서 있는 자세 (Standing):** 
  $y_{shoulder} < y_{pelvis} < y_{knee}$ (어깨가 가장 위, 무릎이 가장 아래)
- **(b) 앉은 자세 (Sitting):**
  어깨는 가장 위에 위치하나, 골반과 무릎의 $y$좌표 차이가 임계값 이내여서 비슷한 선상에 위치할 때.
- **(c) 누워있는 자세 / 낙상 (Lying down):**
  $\vert{}y_{max} - y_{min}\vert{} \le 60$ 
  (어깨, 골반, 무릎의 $y$좌표 최댓값과 최솟값의 차이가 전체 해상도의 10% 인 60픽셀 이내에 모두 모여있을 때)

---

## 📁 디렉토리 구조 (예정)
```text
├── config.py              # 🆕 전체 시스템 환경 설정 및 상수 관리 (하드코딩 방지)
├── main.py                # 루프 및 프레임 딜레이(FPS) 제어, 화면 렌더링
├── camera_manager.py      # 웹캠 프레임 I/O 처리
├── vital_analyzer.py      # rPPG 및 피부 질감 분석 모듈
├── pose_detector.py       # 자세 추정 및 낙상 감지 모듈
└── README.md
